export default interface ThemeInterface {
  primaryColor: string;
  primaryColorInverted: string;
}

export interface Adventures {
  id: string;
  title: string;
  number: number;
  campaign: string;
}