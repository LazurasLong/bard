export default interface ThemeInterface {
  primaryColor: string;
  primaryColorInverted: string;
}

export interface Adventure {
  id: string;
  title: string;
  number: number;
  campaign: string;
}